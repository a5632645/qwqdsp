#include <algorithm>
#include <array>
#include <atomic>
#include <cmath>
#include <cstddef>
#include <cstring>
#include <functional>
#include <iterator>
#include <numbers>
#include <vector>

#include "raylib.h"
#include "../playing/miniaudio.h"
#include "../playing/slider.hpp"

#include <qwqdsp/filter/svf.hpp>
#include <qwqdsp/fx/uniform_convolution.hpp>
#include <qwqdsp/spectral/ipp_real_fft.hpp>
#include <qwqdsp/window/hann.hpp>
#include <qwqdsp/window/blackman.hpp>
#include <qwqdsp/window/helper.hpp>

static constexpr float kSampleRate = 48000.0f;

// Runtime parameters (main thread writes, audio thread reads)
static std::atomic<float> s_min_howl_freq{200.0f};
static std::atomic<float> s_ptpr_threshold{10.0f};
static std::atomic<float> s_papr_threshold{10.0f};
static std::atomic<float> s_q_max{10.0f};
static std::atomic<float> s_gain_min_db{-60.0f};
static std::atomic<float> s_rel_max_ms{20000.0f};
static std::atomic<float> s_attack_ms{10.0f};
static std::atomic<float> s_feedback_gain_db{-80.0f};

// ============================================================
//  模拟扬声器到麦克风的响应 (ported from auto_notch.cpp)
// ============================================================
static constexpr float rir[]{
    0.000266,  -0.002012, 0.000897,  -0.000504, 0.000234,  -0.000172, -0.000203,
    0.000293,  -0.001131, -0.006891, -0.135781, -0.654303, -1.332799, -1.412939,
    -0.838869, -0.222403, 0.065345,  0.251710,  0.296987,  0.020201,  -0.214339,
    -0.067540, 0.177664,  0.237562,  0.253479,  0.330032,  0.361224,  0.302488,
    0.231261,  0.297921,  0.403839,  0.347678,  0.157073,  0.039788,  0.005480,
    -0.027097, 0.048041,  0.185650,  0.209119,  0.114148,  0.023275,  0.012842,
    0.052362,  0.091241,  0.131637,  0.161574,  0.114828,  0.003723,  -0.059357,
    -0.029671, 0.032426,  0.056659,  0.053885,  0.046671,  0.029436,  0.002962,
    0.007076,  0.057421,  0.114610,  0.162291,  0.174986,  0.152472,  0.104757,
    0.075149,  0.105895,  0.160113,  0.172630,  0.124836,  0.070113,  0.036635,
    0.016696,  -0.002383, -0.007322, 0.014652,  0.029488,  0.018084,  0.008707,
    0.025148,  0.040771,  0.048943,  0.062464,  0.072563,  0.067919,  0.051809,
    0.052710,  0.057194,  0.046779,  0.021491,  0.001087,  -0.009784, -0.025210,
    -0.043896, -0.060004, -0.059880, -0.049785, -0.047971, -0.047295, -0.035163,
    -0.022586, -0.021542, -0.004394, 0.024427,  0.030676,  0.006068,  -0.017582,
    -0.020707, -0.020574, -0.022071, -0.027878, -0.032497, -0.039004, -0.048001,
    -0.055676, -0.056284, -0.056698, -0.062355, -0.061060, -0.059525, -0.060332,
    -0.057477, -0.047488, -0.036466, -0.034937, -0.039459, -0.045681, -0.049697,
    -0.048893, -0.046229, -0.044906, -0.050084, -0.053853, -0.059030, -0.062122,
    -0.057322, -0.044817, -0.040379, -0.050975, -0.059985, -0.063800, -0.059996,
    -0.051683, -0.042769, -0.041234, -0.043384, -0.041630, -0.033299, -0.024216,
    -0.017185, -0.008673, -0.005934, -0.014734, -0.034361, -0.050062, -0.054024,
    -0.047871, -0.040265, -0.039977, -0.047861, -0.057887, -0.060643, -0.055123,
    -0.046769, -0.039522, -0.034713, -0.031128, -0.027096, -0.022016, -0.016935,
    -0.008087, 0.003592,  0.010172,  0.006679,  -0.002049, -0.009891, -0.012179,
    -0.006036, 0.000947,  -0.001292, -0.010848, -0.019701, -0.025517, -0.028305,
    -0.026733, -0.020385, -0.010846, -0.001260, 0.006233,  0.010823,  0.014246,
    0.019950,  0.027368,  0.030560,  0.027032,  0.019081,  0.011482,  0.008769,
    0.008530,  0.005398,  0.000254,  -0.003047, -0.005160, -0.006537, -0.004566,
    -0.000697, 0.001556,  0.002082,  0.003590,  0.004902,  0.006491,  0.012691,
    0.023562,  0.033671,  0.036062,  0.032941,  0.028958,  0.025756,  0.024832,
    0.025432,  0.025335,  0.022330,  0.018195,  0.014730,  0.011470,  0.010313,
    0.012011,  0.015326,  0.018151,  0.021639,  0.023962,  0.024008,  0.025517,
    0.030458,  0.033808,  0.032467,  0.029698,  0.028237,  0.026931,  0.026642,
    0.026468,  0.024447,  0.019475,  0.014244,  0.010553,  0.008302,  0.007171,
    0.007208,  0.007767,  0.007516,  0.006590,  0.006763,  0.008792,  0.012270,
    0.016991,  0.022106,  0.025447,  0.025838,  0.024429,  0.023605,  0.022067,
    0.018962,  0.015332,  0.012067,  0.008459,  0.005621,  0.004903,  0.005468,
    0.005397,  0.004835,  0.004727,  0.004994,  0.005670,  0.006643,  0.008211,
    0.009650,  0.010314,  0.010604,  0.010299,  0.009050,  0.007781,  0.006840,
    0.004976,  0.001411,  -0.002352, -0.005030, -0.007044, -0.008653, -0.009272,
    -0.009851, -0.011296, -0.012167, -0.011447, -0.010168, -0.008725, -0.007201,
    -0.006266, -0.006378, -0.006420, -0.005404, -0.003764, -0.002525, -0.001666,
    -0.001053, -0.001231, -0.001964, -0.002776, -0.002991, -0.003876, -0.005412,
    -0.006129, -0.006546, -0.007804, -0.009136, -0.009334, -0.008199, -0.006694,
    -0.005421, -0.004169, -0.003306, -0.003198, -0.003078, -0.003039, -0.003771,
    -0.005763, -0.007631, -0.008776, -0.009957, -0.011546, -0.013531, -0.015529,
    -0.017835, -0.019759, -0.020787, -0.020332, -0.018886, -0.016845, -0.014243,
    -0.011982, -0.010060, -0.007870, -0.005326, -0.002890, -0.001062, -0.000493,
    -0.000980, -0.001146, -0.000986, -0.001228, -0.001713, -0.002220, -0.002579,
    -0.003114, -0.003947, -0.004709, -0.004998, -0.005372, -0.005404, -0.005108,
    -0.004996, -0.004970, -0.004504, -0.003769, -0.003157, -0.002751, -0.002795,
    -0.002912, -0.003179, -0.003918, -0.005099, -0.006208, -0.006720, -0.006991,
    -0.007105, -0.007505, -0.007796, -0.007144, -0.005745, -0.004444, -0.003166,
    -0.001367, 0.000455,  0.001573,  0.002189,  0.002447,  0.002371,  0.002490,
    0.002977,  0.003400,  0.003442,  0.003425,  0.003622,  0.004098,  0.004412,
    0.003976,  0.003193,  0.002805,  0.002144,  0.001311,  0.000845,  0.000601,
    0.000483,  0.001015,  0.002099,  0.002820,  0.003104,  0.003256,  0.003533,
    0.003704,  0.003684,  0.003722,  0.003816,  0.003927,  0.003691,  0.003277,
    0.003051,  0.003262,  0.003596,  0.003475,  0.003035,  0.002732,  0.002334,
    0.002018,  0.002289,  0.002901,  0.003188,  0.003458,  0.004278,  0.005112,
    0.005442,  0.005637,  0.005627,  0.005457,  0.004830,  0.003628,  0.002738,
    0.001773,  0.000914,  0.000592,  0.000466,  0.000249,  0.000212,  0.000761,
    0.001399,  0.001616,  0.001686,  0.001690,  0.001698,  0.001775,  0.001842,
    0.001543,  0.001225,  0.000916,  0.000775,  0.000581,  0.000628,  0.000922,
    0.001262,  0.001112,  0.000834,  0.000452,  -0.000077, -0.000800, -0.001256,
    -0.001733, -0.002360, -0.003613, -0.008245, -0.019978, -0.031495, -0.026634,
    -0.008864, 0.006224,  0.011549,  0.010693,  0.009026,  0.007351,  0.007355,
    0.007717,  0.004491,  -0.002206, -0.008670, -0.007948, 0.000179,  0.005457,
    0.005701,  0.006144,  0.007298,  0.005518,  0.005288,  0.007498,  0.008071,
    0.009094,  0.012407,  0.012905,  0.006702,  -0.002422, -0.008973, -0.010351,
    -0.008113, -0.005002, -0.004974, -0.007590, -0.008800, -0.008644, -0.009458,
    -0.011379, -0.011810, -0.011166, -0.009007, -0.004920, -0.001654, -0.005703,
    -0.018129, -0.017287, 0.000444,  0.014035,  0.016589,  0.012181,  0.009061,
    0.008697,  0.013118,  0.015955,  0.013229,  0.008389,  0.002407,  -0.004831,
    -0.012735, -0.016110, -0.015105, -0.006643, 0.007805,  0.017435,  0.018484,
    0.020181,  0.021570,  0.015220,  0.005480,  -0.000618, -0.004854, -0.008391,
    -0.011093, -0.015977, -0.020907, -0.021068, -0.014484, -0.007188, -0.002731,
    -0.001787, 0.000793,  0.006095,  0.008411,  0.004601,  -0.001062, -0.001015,
    -0.000528, -0.004710, -0.007324, -0.004700, -0.001188, -0.000192, 0.000261,
    0.001067,  0.003593,  0.007778,  0.011402,  0.010970,  0.007055,  0.001771,
    -0.002452, -0.003259, -0.003722, -0.004535, -0.003972, -0.001998, -0.001689,
    -0.002922, -0.001437, 0.001168,  0.002710,  0.002783,  0.001730,  -0.000134,
    -0.000969, -0.000686, 0.000128,  0.000976,  0.000825,  -0.000104, -0.002086,
    -0.003359, -0.003945, -0.004761, -0.004833, -0.003250, -0.000610, 0.001387,
    0.000648,  -0.002595, -0.004586, -0.003097, 0.000911,  0.003739,  0.003953,
    0.002051,  -0.000563, -0.002436, -0.000972, 0.001934,  0.002541,  0.002016,
    0.002816,  0.002206,  -0.000409, -0.001833, -0.001723, -0.002166, -0.003512,
    -0.005262, -0.006375, -0.006136, -0.005782, -0.004438, -0.001724, 0.000530,
    0.000108,  -0.001632, -0.002193, -0.000935, 0.000484,  0.000630,  -0.000867,
    -0.002344, -0.002734, -0.001966, -0.001829, -0.002182, -0.001319, 0.000511,
    0.002180,  0.002988,  0.002092,  0.000393,  0.000631,  0.003441,  0.006277,
    0.006229,  0.003297,  0.001021,  0.000915,  0.001690,  0.001723,  0.000553,
    -0.000973, -0.002056, -0.002176, -0.001371, -0.000641, -0.000589, -0.000028,
    0.001556,  0.002605,  0.002852,  0.002039,  0.000312,  -0.000979, -0.001492,
    -0.001511, -0.001247, -0.001934, -0.003666, -0.004375, -0.002980, -0.001034,
    0.000706,  0.002295,  0.003458,  0.004257,  0.004238,  0.004624,  0.005034,
    0.004674,  0.003597,  0.002890,  0.002573,  0.001853,  0.001342,  0.001263,
    0.001374,  0.001544,  0.000979,  -0.000574, -0.001938, -0.002114, -0.001216,
    -0.000654, -0.001037, -0.001418, -0.001776, -0.001568, -0.000172, 0.001645,
    0.002457,  0.002500,  0.002662,  0.002288,  0.001373,  0.000230,  -0.000407,
    -0.000578, -0.000815, -0.000835, -0.001041, -0.001653, -0.001856, -0.001156,
    -0.000319, -0.000392, -0.000847, -0.000928, -0.000557, -0.000381, -0.000575,
    -0.000944, -0.001200, -0.001022, -0.000049, 0.000515,  0.000429,  -0.000144,
    -0.000376, 0.000213,  0.000798,  0.000635,  0.000035,  -0.000592, -0.000724,
    -0.000810, -0.000721, -0.000744, -0.000575, 0.000368,  0.001509,  0.002683,
    0.003393,  0.003140,  0.002659,  0.002309,  0.001612,  0.000686,  0.000233,
    0.000594,  0.000961,  0.001091,  0.001255,  0.001060,  0.000220,  -0.000662,
    -0.000944, -0.000080, 0.001131,  0.001396,  0.000935,  -0.000098, -0.001309,
    -0.002741, -0.003486, -0.003029, -0.001957, -0.001239, -0.000673, 0.000016,
    0.000754,  0.001103,  0.001498,  0.002021,  0.002236,  0.001487,  0.000540,
    0.000103,  0.000032,  0.000115,  0.000343,  0.000695,  0.001104,  0.001551,
    0.001831,  0.001909,  0.001783,  0.001546,  0.001518,  0.001886,  0.002235,
    0.001823,  0.000954,  0.000547,  0.000632,  0.000850,  0.000256,  -0.000489,
    -0.000728, -0.000552, -0.000439, -0.000088, -0.000024, -0.000636, -0.001582,
    -0.001869, -0.001529, -0.001012, -0.000423, 0.000591,  0.000825,  0.000130,
    -0.000580, -0.000833, -0.001106, -0.001415, -0.001472, -0.001507, -0.001937,
    -0.002333, -0.002080, -0.001359, -0.000761, -0.000144, -0.000006, -0.000248,
    -0.000770, -0.001063, -0.000940, -0.000421, -0.000354, -0.001154, -0.002335,
    -0.003250, -0.003831, -0.003772, -0.002634, -0.001533, -0.000830, -0.000538,
    -0.000684, -0.001066, -0.000815, -0.000154, 0.000999,  0.002233,  0.002890,
    0.002259,  0.001272,  0.000580,  0.000464,  0.000896,  0.002124,  0.002930,
    0.002824,  0.002306,  0.001729,  0.001062,  -0.000356, -0.001442, -0.001072,
    0.000003,  0.000855,  0.001233,  0.001495,  0.002190,  0.003420,  0.003508,
    0.001864,  -0.000136, -0.001064, -0.000624, 0.000628,  0.001427,  0.001122,
    0.000408,  -0.000086, -0.000439, -0.000624, -0.001471, -0.002647, -0.003467,
    -0.004649, -0.006625, -0.007494, -0.005120, 0.000653,  0.007566,  0.012779,
    0.014098,  0.011401,  0.006887,  0.003580,  0.002710,  0.002490,  0.002164,
    0.002349,  0.002935,  0.003061,  0.002424,  0.001425,  0.000787,  0.000376,
    0.000599,  0.001130,  0.000813,  -0.000541, -0.001403, -0.001668, -0.002181,
    -0.002516, -0.001623, -0.000509, -0.000188, -0.000447, -0.000617, -0.001150,
    -0.002895, -0.004748, -0.005243, -0.005153, -0.005207, -0.004818, -0.003803,
    -0.003141, -0.003084, -0.002700, -0.001968, -0.001908, -0.002395, -0.002341,
    -0.001574, -0.001449, -0.002250, -0.002965, -0.003324, -0.003310, -0.003068,
    -0.002219, -0.001326, -0.001036, -0.000247, 0.000949,  0.000976,  0.000010,
    -0.000866, -0.000987, -0.001294, -0.001771, -0.001854, -0.001610, -0.000902,
    -0.000283, -0.000085, -0.000464, -0.001901, -0.003356, -0.004108, -0.004777,
    -0.004949, -0.004010, -0.002777, -0.002287, -0.002311, -0.002565, -0.002457,
    -0.001746, -0.000453, 0.001263,  0.002554,  0.003443,  0.003791,  0.004011,
    0.004184,  0.003673,  0.003119,  0.003430,  0.003756,  0.003583,  0.003401,
    0.003336,  0.003442,  0.003725,  0.004164,  0.004186,  0.003813,  0.002673,
    0.001449,  0.001020,  0.000948,  0.000379,  -0.000512, -0.001389, -0.002494,
    -0.003646, -0.003688, -0.002777, -0.002341, -0.001968, -0.001324, -0.001753,
    -0.002685, -0.002714, -0.002304, -0.002247, -0.002043, -0.001230, 0.000097,
    0.001472,  0.002394,  0.002865,  0.003389,  0.003602,  0.003224,  0.002844,
    0.002706,  0.003297,  0.004419,  0.005122,  0.004594,  0.003191,  0.001954,
    0.001045,  0.000975,  0.000786,  0.000842,  0.001265,  0.001320,  0.001010,
    0.001028,  0.001834,  0.002656,  0.003108,  0.002624,  0.001615,  0.000763,
    0.000435,  0.000640,  0.001268,  0.001998,  0.001663,  0.000323,  -0.000988,
    -0.001742, -0.001540, -0.000748, -0.000720, -0.001246, -0.001694, -0.002030,
    -0.001679, 0.001857,  0.005531,  0.005454,  0.002987,  0.001012,  0.000069,
    -0.001047, -0.002065, -0.002228, -0.000911, 0.000665,  0.000800,  -0.000527,
    -0.001008, 0.000081,  0.001930,  0.003612,  0.004493,  0.003250,  0.000236,
    -0.002895, -0.004194, -0.003522, -0.002875, -0.003083, -0.002763, -0.001948,
    -0.001988, -0.002912, -0.003207, -0.002516, -0.001642, -0.000685, 0.000111,
    -0.000102, -0.001609, -0.003026, -0.003598, -0.003487, -0.002706, -0.001224,
    -0.000063, 0.000394,  0.000114,  -0.000266, 0.000005,  0.000541,  0.000340,
    0.000022,  -0.000581, -0.001892, -0.003208, -0.003613, -0.003579, -0.003743,
    -0.003690, -0.002156, 0.000115,  0.002223,  0.003669,  0.004317,  0.004089,
    0.003701,  0.003790,  0.003865,  0.002859,  0.001753,  0.001414,  0.001033,
    -0.000292, -0.001448, -0.001019, 0.000696,  0.002749,  0.004076,  0.004058,
    0.002658,  0.001423,  0.001054,  0.001453,  0.001330,  0.000129,  -0.001595,
    -0.002919, -0.003428, -0.003247, -0.002408, -0.000998, -0.000296, -0.000425,
    -0.001335, -0.002062, -0.002060, -0.001660, -0.001273, -0.000929, -0.000693,
    -0.000991, -0.001554, -0.001511, -0.001624, -0.001881, -0.002493, -0.002223,
    -0.001316, -0.000481, 0.000246,  0.000911,  0.001878,  0.002509,  0.003062,
    0.003181,  0.002726,  0.002266,  0.001541,  0.000650,  0.000106,  0.000606,
    0.002126,  0.003681,  0.003711,  0.002209,  0.000521,  -0.000283, -0.000454,
    -0.000261, -0.000439, -0.001190, -0.001924, -0.002536, -0.002958, -0.003101,
    -0.002609, -0.001841, -0.001268, -0.001214, -0.001374, -0.001465, -0.001413,
    -0.000926, 0.000078,  0.001155,  0.001643,  0.000945,  -0.000090, -0.000545,
    -0.000144, -0.000230, -0.000349, 0.000802,  0.002241,  0.003147,  0.003207,
    0.003413,  0.003429,  0.002719,  0.001743,  0.001036,  0.000268,  -0.000344,
    -0.000320, 0.000283,  0.000525,  0.000410,  0.000419,  0.000381,  0.000174,
    -0.000173, -0.000289, 0.000102,  0.000936,  0.001119,  0.000386,  -0.000908,
    -0.001742, -0.001432, -0.000560, 0.000364,  0.001269,  0.002071,  0.002332,
    0.001636,  0.000611,  -0.000414, -0.000970, -0.001375, -0.001345, -0.001070,
    -0.000912, -0.001112, -0.001131, -0.000613, 0.000005,  0.000277,  0.000305,
    0.000294,  0.000398,  0.000354,  0.000307,  -0.000390, -0.001497, -0.002283,
    -0.001894, -0.000825, 0.000564,  0.002039,  0.003361,  0.003616,  0.003011,
    0.002268,  0.001441,  0.000241,  -0.000514, -0.000598, -0.000262, 0.000103,
    0.000080,  -0.000050, -0.000179, -0.000431, -0.000863, -0.001053, -0.000722,
    0.000090,  0.000863,  0.001227,  0.001085,  0.001279,  0.001332,  0.000368,
    -0.001560, -0.003079, -0.003325, -0.002992, -0.002047, -0.000444, 0.001014,
    0.001430,  0.001465,  0.001806,  0.002103,  0.001898,  0.000928,  -0.000062,
    -0.000694, -0.000889, -0.000661, -0.000315, -0.000215, -0.000446, -0.000996,
    -0.001513, -0.002077, -0.002210, -0.001736, -0.000950, -0.000556, -0.000711,
    -0.000937, -0.000758, 0.000054,  0.001254,  0.002111,  0.002046,  0.001346,
    0.000380,  -0.000364, -0.000509, -0.000317, 0.000023,  0.000415,  0.000094,
    -0.000620, -0.001107, -0.000595, 0.000257,  0.000350,  -0.000052, -0.000518,
    -0.000941, -0.001057, -0.000715, 0.000127,  0.000744,  0.000614,  -0.000030,
    -0.000880, -0.001548, -0.001817, -0.001184, -0.000384, 0.000119,  -0.000262,
    -0.000990, -0.001436, -0.001487, -0.001880, -0.002615, -0.003278, -0.003734,
    -0.003852, -0.003273, -0.002545, -0.001889, -0.001339, -0.000792, -0.000368,
    -0.000372, -0.000238, -0.000056, 0.000290,  0.000273,  -0.000279, -0.000714,
    -0.000645, -0.000260, 0.000429,  0.001250,  0.001912,  0.001908,  0.001459,
    0.000637,  0.000083,  -0.000153, 0.000145,  0.000462,  0.000423,  0.000242,
    0.000431,  0.000865,  0.001176,  0.000975,  0.000488,  -0.000634, -0.001516,
    -0.001802, -0.001655, -0.001129, -0.000654, -0.000725, -0.001075, -0.001449,
    -0.001783, -0.001268, -0.000040, 0.000829,  0.001087,  0.001316,  0.001778,
    0.002032,  0.002069,  0.002283,  0.002516,  0.002374,  0.002136,  0.002171,
    0.002033,  0.001717,  0.001297,  0.000683,  0.000029,  -0.000347, -0.000015,
    0.000706,  0.001260,  0.001725,  0.002237,  0.002554,  0.002363,  0.001418,
    0.000471,  0.000035,  0.000129,  0.000820,  0.001461,  0.001295,  0.000464,
    -0.000532, -0.000855, -0.000212, 0.001317,  0.002650,  0.003146,  0.002561,
    0.001169,  -0.000213, -0.000428, 0.000371,  0.001835,  0.003068,  0.003455,
    0.003102,  0.002606,  0.001950,  0.001373,  0.000720,  -0.000098, -0.000771,
    -0.000825, -0.000881, -0.000811, -0.000477, -0.000029, -0.000084, -0.000459,
    -0.001111, -0.001631, -0.001732, -0.001612, -0.001319, -0.001105, -0.000840,
    -0.000268, 0.000258,  0.000492,  0.000301,  0.000121,  0.000035,  -0.000051,
    -0.000322, -0.000919, -0.001718, -0.002436, -0.002832, -0.002644, -0.002049,
    -0.001618, -0.001110, -0.000396, -0.000169, -0.000411, -0.000789, -0.000777,
    -0.000341, 0.000358,  0.000826,  0.000910,  0.000487,  -0.000357, -0.001137,
    -0.001392, -0.001245, -0.001410, -0.001496, -0.001433, -0.001221, -0.000844,
    -0.000534, -0.000380, -0.000502, -0.000914, -0.001285, -0.001413, -0.001254,
    -0.001158, -0.001288, -0.001658, -0.002160, -0.002034, -0.001619, -0.001375,
    -0.001211, -0.001161, -0.001390, -0.001536, -0.001767, -0.001999, -0.001777,
    -0.001018, 0.000110,  0.001025,  0.001360,  0.001437,  0.001510,  0.001414,
    0.001651,  0.001807,  0.001409,  0.001173,  0.001326,  0.001362,  0.000893,
    0.000432,  0.000191,  0.000024,  0.000008,  0.000148,  0.000005,  -0.000416,
    -0.000722, -0.000676, -0.000639, -0.000479, -0.000343, 0.000192,  0.000789,
    0.001165,  0.001040,  0.001024,  0.000983,  0.001281,  0.001610,  0.001680,
    0.001268,  0.001006,  0.000956,  0.000988,  0.001354,  0.001607,  0.001898,
    0.002496,  0.003088,  0.002779,  0.001756,  0.001199,  0.001066,  0.000415,
    -0.000308, -0.000711, -0.001291, -0.002196, -0.002642, -0.002320, -0.002184,
    -0.002210, -0.002489, -0.002434, -0.002040, -0.001261, -0.000587, -0.000081,
    0.000044,  -0.000329, -0.000467, -0.000161, 0.000092,  -0.000233, -0.000262,
    -0.000036, 0.000344,  0.000382,  0.000375,  0.000361,  0.000418,  0.000468,
    0.000656,  0.001217,  0.001642,  0.001903,  0.001939,  0.001786,  0.001114,
    0.000720,  0.000590,  0.001009,  0.001250,  0.001053,  0.000720,  0.000702,
    0.000700,  0.000605,  0.000459,  0.000662,  0.000993,  0.000902,  0.000667,
    0.000552,  0.000393,  0.000344,  0.000061,  -0.000482, -0.001407, -0.001931,
    -0.001569, -0.000841, -0.000235, 0.000126,  0.000362,  0.000361,  0.000443,
    0.000233,  -0.000262, -0.000640, -0.000354, 0.000026,  0.000120,  -0.000374,
    -0.000869, -0.000996, -0.000698, -0.000123, 0.000370,  0.000518,  0.000396,
    -0.000125, -0.000707, -0.000816, -0.000600, -0.000757, -0.001176, -0.001545,
    -0.001424, -0.001123, -0.000810, -0.000112, 0.000862,  0.001237,  0.000973,
    0.000619,  0.000337,  0.000230,  0.000392,  0.000458,  0.000260,  -0.000229,
    -0.000332, 0.000184,  0.001013,  0.001174,  0.000616,  -0.000135, -0.000677,
    -0.001232, -0.001941, -0.002146, -0.001993, -0.001752, -0.001442, -0.001221,
    -0.000972, -0.000923, -0.000918, -0.000751, -0.000356, -0.000021, 0.000653,
    0.000904,  0.000901,  0.000793,  0.000881,  0.001123,  0.001424,  0.001613,
    0.001462,  0.001106,  0.000946,  0.000837,  0.000687,  0.000518,  0.000575,
    0.000523,  0.000347,  0.000377,  0.000305,  -0.000077, -0.000311, -0.000301,
    -0.000120, -0.000137, -0.000460, -0.000762, -0.000870, -0.000856, -0.000925,
    -0.000850, -0.000728, -0.000571, -0.000485, -0.000238, -0.000156, -0.000347,
    -0.000388, 0.000113,  0.000346,  0.000538,  0.000587,  0.000593,  -0.000026,
    -0.000792, -0.001201, -0.001295, -0.001502, -0.001742, -0.001872, -0.001558,
    -0.001096, -0.001043, -0.001046, -0.000845, -0.000724, -0.000750, -0.000452,
    -0.000044, 0.000210,  0.000466,  0.000681,  0.000712,  0.000231,  0.000015,
    0.000052,  0.000296,  0.000362,  0.000186,  -0.000018, -0.000129, 0.000014,
    0.000320,  0.000553,  0.000615,  0.000413,  0.000406,  0.000476,  0.000530,
    0.000388,  0.000321,  0.000048,  -0.000120, -0.000078, 0.000309,  0.000269,
    -0.000014, -0.000019, 0.000387,  0.000753,  0.000882,  0.000899,  0.000944,
    0.001072,  0.001146,  0.001376,  0.001232,  0.000883,  0.000710,  0.000866,
    0.000935,  0.000745,  0.000322,  -0.000141, -0.000371, -0.000564, -0.000795,
    -0.000770, -0.000500, -0.000019, 0.000440,  0.000474,  0.000429,  0.000182,
    0.000060,  0.000001,  -0.000242, -0.000662, -0.000965, -0.001013, -0.000964,
    -0.000726, -0.000525, -0.000178, 0.000172,  0.000401,  0.000264,  0.000070,
    0.000078,  0.000060,  -0.000181, -0.000448, -0.000507, -0.000422, -0.000589,
    -0.000690, -0.000885, -0.000887, -0.000858, -0.000754, -0.000476, -0.000127,
    0.000003,  -0.000078, -0.000487, -0.000767, -0.001087, -0.001158, -0.000859,
    -0.000461, -0.000396, -0.000805, -0.001119, -0.000982, -0.000850, -0.000722,
    -0.000552, -0.000523, -0.000843, -0.001312, -0.001359, -0.001018, -0.000765,
    -0.000492, -0.000552, -0.000600, -0.000543, -0.000012, 0.000754,  0.001164,
    0.001196,  0.001074,  0.000980,  0.000859,  0.001143,  0.001586,  0.001886,
    0.001674,  0.001115,  0.000755,  0.000779,  0.000973,  0.000930,  0.000583,
    0.000323,  -0.000099, -0.000382, -0.000423, -0.000530, -0.000559, -0.000248,
    0.000198,  0.000520,  0.000682,  0.000734,  0.000484,  0.000137,  0.000122,
    0.000310,  0.000711,  0.001097,  0.001408,  0.001270,  0.000829,  0.000521,
    0.000521,  0.000967,  0.001304,  0.001581,  0.001541,  0.001310,  0.001115,
    0.000924,  0.000834,  0.000610,  0.000223,  -0.000425, -0.000930, -0.001424,
    -0.001464, -0.000999, -0.000552, -0.000437, -0.000123, 0.000330,  0.000976,
    0.001333,  0.001490,  0.001082,  0.000420,  -0.000013, -0.000297, -0.000667,
    -0.001168, -0.001580, -0.002002, -0.002027, -0.002038, -0.001988, -0.001836,
    -0.001733, -0.001578, -0.001593, -0.001659, -0.001984, -0.002055, -0.001870,
    -0.001739, -0.001461, -0.001053, -0.000743, -0.000598, -0.000760, -0.000984,
    -0.001099, -0.001147, -0.001067, -0.000878, -0.000521, -0.000484, -0.000940,
    -0.001518, -0.001615, -0.001160, -0.000564, -0.000145, 0.000041,  -0.000120,
    -0.000437, -0.000605, -0.000729, -0.001025, -0.001616, -0.002021, -0.002164,
    -0.001788, -0.001079, -0.000368, 0.000299,  0.000925,  0.001163,  0.000677,
    0.000112,  -0.000417, -0.001134, -0.001444, -0.001344, -0.001023, -0.000962,
    -0.000914, -0.000833, -0.000767, -0.000669, -0.000453, 0.000041,  0.000542,
    0.000991,  0.001149,  0.001256,  0.001200,  0.000925,  0.000855,  0.001224,
    0.001613,  0.001802,  0.001809,  0.001482,  0.000894,  0.000533,  0.000619,
    0.001037,  0.001158,  0.001139,  0.001003,  0.000757,
};

// ============================================================
//  Howling suppression engine (ported from auto_notch.cpp)
// ============================================================
class AutoNotch {
public:
    static constexpr int fft_size = 1024;
    static constexpr int slen = fft_size / 2;
    static constexpr int hop_size = slen / 2;
    static constexpr int ipmp_window = 3;
    static constexpr int max_notches = 16;
    static constexpr bool use_fix_release = false;

    int GetNumActive() const noexcept {
        int n = 0;
        for (auto& ns : notch_states)
            if (ns.active) ++n;
        return n;
    }

    float GetNotchFreq(int idx) const noexcept {
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (idx == 0) return ns.w * sr_ / (2.0f * std::numbers::pi_v<float>);
            --idx;
        }
        return 0;
    }

    float GetNotchGain(int idx) const noexcept {
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (idx == 0) return ns.peak_gain_db;
            --idx;
        }
        return 0;
    }

    int GetNotchRelease(int idx) const noexcept {
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (idx == 0) return ns.release_counter;
            --idx;
        }
        return 0;
    }

    float GetNotchQ(int idx) const noexcept {
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (idx == 0) return ns.q;
            --idx;
        }
        return 0;
    }

    int GetNotchTotalRelease(int idx) const noexcept {
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (idx == 0) return ns.total_release_frames;
            --idx;
        }
        return 0;
    }

    AutoNotch() {
        qwqdsp_window::Blackman::Window({window, slen}, true);
        norm_gain_ = qwqdsp_window::Helper::NormalizeGain({window, slen});
        fft.Init(fft_size);
    }

    void Init(float sr) noexcept {
        sr_ = sr;
        // 100ms exponential smoothing for spectrum display
        spec_smooth_alpha_ = 1.0f - std::exp(-hop_size / (sr * 0.1f));
    }

    void Reset() noexcept {
        frame_pos = 0;
        frame_id = 0;
        for (int i = 0; i < ipmp_window; ++i) {
            for (int j = 0; j < slen; ++j) {
                candidates[i][j] = false;
            }
        }
        for (int i = 0; i < slen; ++i) {
            candidate_count[i] = 0;
        }
        howl_bins_.clear();
        out_frame_pos = 0;
        std::fill_n(spec_smooth_before_, slen, -80.0f);
        std::fill_n(spec_smooth_after_, slen, -80.0f);
        for (NotchState& n : notch_states) {
            n.filter.Reset();
            n.bin = 0;
            n.w = 0;
            n.q = 0;
            n.peak_gain_db = 0;
            n.original_prominence_db = 0;
            n.total_release_frames = 0;
            n.release_counter = 0;
            n.attack_counter = 0;
            n.active = false;
            n.matched_this_frame = false;
        }
    }

    void Process(float* x, int frames) {
        while (frames != 0) {
            int i_need = slen - frame_pos;
            int can_do = std::min(i_need, frames);
            std::copy_n(x, can_do, frame_buf + frame_pos);
            frame_pos += can_do;
            frames -= can_do;

            if (frame_pos == slen) {
                FindHowling();
                UpdateNotches();
            }
            
            // Apply cascaded notch filters with release time
            for (auto& ns : notch_states) {
                if (ns.active) {
                    for (int i = 0; i < can_do; ++i) {
                        x[i] = ns.filter.Tick(x[i]);
                    }
                }
            }

            // Collect output samples for spectrum display
            std::copy_n(x, can_do, out_frame_buf + out_frame_pos);
            out_frame_pos += can_do;
            if (out_frame_pos >= slen) {
                float fft_in[fft_size]{};
                for (int i = 0; i < slen; ++i)
                    fft_in[i] = out_frame_buf[i] * window[i];
                fft.FFT(fft_in, out_fft_out_);
                // Update smoothed after-spectrum (100ms EMA)
                for (int i = 0; i < slen; ++i) {
                    float g = (i == 0) ? norm_gain_ * 0.5f : norm_gain_;
                    float raw = 20.0f * std::log10(get_magnitude(out_fft_out_, i) * g + 1e-30f);
                    spec_smooth_after_[i] += spec_smooth_alpha_ * (raw - spec_smooth_after_[i]);
                }
                out_frame_pos = 0;
            }

            x += can_do;
        }
    }

    // Spectrum accessors for GUI (100ms EMA smoothed, normalized 0dBFS)
    float GetSpectrumBefore(int bin) const noexcept {
        return spec_smooth_before_[bin];
    }
    float GetSpectrumAfter(int bin) const noexcept {
        return spec_smooth_after_[bin];
    }
    static constexpr int GetSlen() noexcept { return slen; }
private:
    float norm_gain_{};
    float spec_smooth_alpha_{};
    float spec_smooth_before_[slen]{};
    float spec_smooth_after_[slen]{};
    float sr_{};
    float frame_buf[slen]{};
    int frame_pos = 0;
    int frame_id = 0;

    bool candidates[slen][ipmp_window]{};
    int candidate_count[slen]{};
    float window[slen];
    qwqdsp_spectral::IppRealFFT fft;

    struct NotchState {
        qwqdsp_filter::SVF filter;
        int bin{};
        float w{};
        float q{};
        float peak_gain_db{};
        float original_prominence_db{};  // prominence when this notch was last assigned
        int total_release_frames{};
        int release_counter{};
        int attack_counter{};
        bool active{};
        bool matched_this_frame{};
    };
    std::vector<int> howl_bins_;
    float fft_out_[fft_size + 2]{};
    float out_frame_buf[slen]{};
    int out_frame_pos = 0;
    float out_fft_out_[fft_size + 2]{};
    std::array<NotchState, max_notches> notch_states;

    // ── helpers ──
    float get_power(const float* fft_out, int bin) const noexcept {
        if (bin == 0) return fft_out[0] * fft_out[0];
        return fft_out[2 * bin] * fft_out[2 * bin] + fft_out[2 * bin + 1] * fft_out[2 * bin + 1];
    }

    float get_magnitude(const float* fft_out, int bin) const noexcept {
        if (bin == 0) return std::abs(fft_out[0]);
        return std::sqrt(fft_out[2 * bin] * fft_out[2 * bin] + fft_out[2 * bin + 1] * fft_out[2 * bin + 1]);
    }

    std::vector<int> ptpr(const float* fft_out, int slen, float threshold_db) noexcept {
        std::vector<int> result;
        float ng_sq = norm_gain_ * norm_gain_;
        for (int i = 0; i < slen; ++i) {
            float power = get_power(fft_out, i);
            float norm_power = (i == 0) ? power * ng_sq * 0.25f : power * ng_sq;
            if (10.0f * std::log10(norm_power + 1e-30f) > threshold_db)
                result.push_back(i);
        }
        return result;
    }

    std::vector<int> papr(const float* fft_out, int slen, float threshold_db) noexcept {
        float avg_power = 0.0f;
        for (int i = 0; i < slen; ++i)
            avg_power += get_power(fft_out, i);
        avg_power /= static_cast<float>(slen);

        std::vector<int> result;
        for (int i = 0; i < slen; ++i) {
            if (10.0f * std::log10(get_power(fft_out, i) / (avg_power + 1e-30f) + 1e-30f) > threshold_db)
                result.push_back(i);
        }
        return result;
    }

    std::vector<int> pnpr(const float* fft_out, int slen, float threshold_db) noexcept {
        std::vector<int> result;
        for (int i = 2; i < slen - 2; ++i) {
            float p = get_power(fft_out, i);
            if (10.0f * std::log10(p / (get_power(fft_out, i - 1) + 1e-30f)) > threshold_db &&
                10.0f * std::log10(p / (get_power(fft_out, i - 2) + 1e-30f)) > threshold_db &&
                10.0f * std::log10(p / (get_power(fft_out, i + 2) + 1e-30f)) > threshold_db &&
                10.0f * std::log10(p / (get_power(fft_out, i + 1) + 1e-30f)) > threshold_db)
                result.push_back(i);
        }
        return result;
    }

    std::vector<int> intersect_sorted(const std::vector<int>& a, const std::vector<int>& b) noexcept {
        std::vector<int> result;
        std::set_intersection(a.begin(), a.end(), b.begin(), b.end(), std::back_inserter(result));
        return result;
    }

    std::vector<int> screening(const float* fft_out, const std::vector<int>& ipmp_candidates, int) noexcept {
        std::vector<int> result;
        for (int c : ipmp_candidates) {
            if (result.empty()) {
                result.push_back(c);
            } else if (result.back() > c - 3) {
                if (get_magnitude(fft_out, result.back()) < get_magnitude(fft_out, c))
                    result.back() = c;
            } else {
                result.push_back(c);
            }
        }
        return result;
    }

    float quadratic_interpolate_bin(const float* fft_out, int peak_bin, int slen) noexcept {
        if (peak_bin <= 0 || peak_bin >= slen - 1)
            return static_cast<float>(peak_bin);

        float y0 = 10.0f * std::log10(get_power(fft_out, peak_bin) + 1e-30f);
        float ym1 = 10.0f * std::log10(get_power(fft_out, peak_bin - 1) + 1e-30f);
        float yp1 = 10.0f * std::log10(get_power(fft_out, peak_bin + 1) + 1e-30f);

        float denom = ym1 + yp1 - 2.0f * y0;
        if (std::abs(denom) < 1e-10f)
            return static_cast<float>(peak_bin);

        float delta = 0.5f * (ym1 - yp1) / denom;
        return static_cast<float>(peak_bin) + std::clamp(delta, -0.5f, 0.5f);
    }

    void FindHowling() {
        float fft_in[fft_size]{};
        float fft_out[fft_size + 2];
        for (int i = 0; i < slen; ++i) {
            fft_in[i] = frame_buf[i] * window[i];
        }
        fft.FFT(fft_in, fft_out);

        // === Howling Detection ===
        auto ptpr_idx = ptpr(fft_out, slen, s_ptpr_threshold.load(std::memory_order_relaxed));
        auto papr_idx = papr(fft_out, slen, s_papr_threshold.load(std::memory_order_relaxed));
        // auto pnpr_idx = pnpr(fft_out, slen, 15.0f);

        auto intersection = intersect_sorted(ptpr_idx, papr_idx);
        // intersection = intersect_sorted(intersection, pnpr_idx);

        int col = frame_id % ipmp_window;
        for (int i = 0; i < slen; ++i) {
            if (candidates[i][col]) {
                candidate_count[i]--;
                candidates[i][col] = false;
            }
        }
        for (int idx : intersection) {
            candidates[idx][col] = true;
            candidate_count[idx]++;
        }

        std::vector<int> ipmp_result;
        for (int i = 0; i < slen; ++i)
            if (candidate_count[i] >= 3)
                ipmp_result.push_back(i);

        auto freq_ids = screening(fft_out, ipmp_result, slen);
        howl_bins_ = std::move(freq_ids);
        std::copy_n(fft_out, fft_size + 2, fft_out_);
        // Update smoothed before-spectrum (100ms EMA)
        for (int i = 0; i < slen; ++i) {
            float g = (i == 0) ? norm_gain_ * 0.5f : norm_gain_;
            float raw = 20.0f * std::log10(get_magnitude(fft_out_, i) * g + 1e-30f);
            spec_smooth_before_[i] += spec_smooth_alpha_ * (raw - spec_smooth_before_[i]);
        }
    }

    void UpdateNotches() {
        float avg_power = 0.0f;
        for (int i = 0; i < slen; ++i)
            avg_power += get_power(fft_out_, i);
        avg_power /= static_cast<float>(slen);

        // Mark all notches as unmatched at start of this frame
        for (auto& ns : notch_states)
            ns.matched_this_frame = false;

        // Single loop over howl bins: assign each to a notch
        for (int fid : howl_bins_) {
            // Compute filter parameters for this bin
            float frac_bin = quadratic_interpolate_bin(fft_out_, fid, slen);
            float w = frac_bin * 2.0f * std::numbers::pi_v<float> / fft_size;
            if (w * sr_ / (2.0f * std::numbers::pi_v<float>) < s_min_howl_freq.load(std::memory_order_relaxed))
                continue;

            float peak_power = get_power(fft_out_, fid);
            float prominence_db = 10.0f * std::log10((peak_power + 1e-30f) / (avg_power + 1e-30f));
            float t = std::clamp((prominence_db - 5.0f) / 25.0f, 0.0f, 1.0f);

            float q_max = s_q_max.load(std::memory_order_relaxed);
            float q = 1.0f + (q_max - 1.0f) * t;

            float gain_min_db = s_gain_min_db.load(std::memory_order_relaxed);
            float gain_db = -20.0f + (gain_min_db + 20.0f) * t;

            float rel_max_ms = s_rel_max_ms.load(std::memory_order_relaxed);
            float rel_ms = 100.0f + (rel_max_ms - 100.0f) * t;
            if constexpr (use_fix_release) rel_ms = 20000.0f;
            int rel_frames = static_cast<int>(rel_ms * sr_ / (hop_size * 1000));
            if (rel_frames < 1) rel_frames = 1;

            // ── Find a notch to assign this howl bin to ──
            NotchState* target = nullptr;

            // Case 1: match existing active notch by integer bin (-1 ~ +1)
            for (auto& ns : notch_states) {
                if (ns.active && !ns.matched_this_frame && std::abs(ns.bin - fid) <= 5) {
                    target = &ns;
                    break;
                }
            }

            // Case 2: use an inactive slot
            if (!target) {
                for (auto& ns : notch_states) {
                    if (!ns.active) {
                        target = &ns;
                        break;
                    }
                }
            }

            // Case 3: steal the active notch with smallest release_counter,
            // but only if current prominence exceeds the notch's original prominence
            if (!target) {
                int min_rel = std::numeric_limits<int>::max();
                NotchState* best_candidate = nullptr;
                for (auto& ns : notch_states) {
                    if (ns.active && ns.release_counter < min_rel) {
                        min_rel = ns.release_counter;
                        best_candidate = &ns;
                    }
                }
                if (best_candidate && prominence_db > best_candidate->original_prominence_db)
                    target = best_candidate;
            }

            // Assign computed parameters to target
            if (target) {
                target->active = true;
                target->matched_this_frame = true;
                target->bin = fid;
                target->w = w;
                target->q = q;
                target->peak_gain_db = gain_db;
                target->original_prominence_db = prominence_db;
                target->total_release_frames = rel_frames;
                target->release_counter = rel_frames;
                target->attack_counter = 0;
            }
        }

        // Attack + Release loop: update counters and gain every frame
        int attack_frames = std::max(1, static_cast<int>(s_attack_ms.load(std::memory_order_relaxed) * sr_ / (hop_size * 1000)));
        for (auto& ns : notch_states) {
            if (!ns.active) continue;
            if (!ns.matched_this_frame) {
                ns.release_counter--;
            }
            ns.attack_counter++;
            // Gain: attack ramps 0dB → peak, release fades peak → 0dB
            float attack_t = std::min(1.0f, static_cast<float>(ns.attack_counter) / attack_frames);
            float release_t = static_cast<float>(ns.release_counter) / ns.total_release_frames;
            float current_gain = ns.peak_gain_db * attack_t * release_t;
            ns.filter.MakeBell(ns.w, ns.q, current_gain);
            // Remove if fully released
            if (ns.release_counter <= 0)
                ns.active = false;
        }

        // Shift frame buffer for next hop
        std::memmove(frame_buf, frame_buf + hop_size, (slen - hop_size) * sizeof(float));
        frame_pos = slen - hop_size;
        frame_id++;
    }
};

// ============================================================
//  Constants
// ============================================================
static constexpr int kWindowWidth  = 800;
static constexpr int kWindowHeight = 480;

// ============================================================
//  Global state (audio callback reads/writes)
// ============================================================
static AutoNotch g_notch;
static std::atomic<bool> g_capturing{false};
static qwqdsp_fx::UniformConvolution s_feedback_conv;
static std::vector<float> s_feedback_buf;

// ============================================================
//  miniaudio callback
// ============================================================
extern "C" void MaCallback(ma_device* pDevice, void* pOutput,
                           const void* pInput, ma_uint32 frameCount) {
    (void)pDevice;

    auto* input  = static_cast<const float*>(pInput);
    auto* output = static_cast<float*>(pOutput);

    if (input == nullptr || output == nullptr) return;

    // Copy input to output, then add RIR-convolved feedback
    std::copy_n(input, frameCount, output);
    float fb_linear = std::pow(10.0f, s_feedback_gain_db.load(std::memory_order_relaxed) / 20.0f);
    size_t min_fb = std::min<size_t>(frameCount, s_feedback_buf.size());
    for (size_t i = 0; i < min_fb; ++i)
        output[i] = std::clamp(output[i] + s_feedback_buf[i] * fb_linear, -1.0f, 1.0f);

    // Apply howling suppression in-place
    g_notch.Process(output, frameCount);

    // Convolve output with room impulse response to generate feedback for next frame
    s_feedback_buf.resize(frameCount);
    std::copy_n(output, frameCount, s_feedback_buf.begin());
    s_feedback_conv.Process(s_feedback_buf);
}

// ============================================================
//  main
// ============================================================
int main(void) {
    SetConfigFlags(FLAG_MSAA_4X_HINT);
    InitWindow(kWindowWidth, kWindowHeight, "Auto Notch — Howling Suppression");
    SetTargetFPS(60);

    g_notch.Init(kSampleRate);
    g_notch.Reset();

    // ── init feedback convolution with room impulse response ──
    s_feedback_conv.Init(512);
    std::vector<float> rir_mut(std::begin(rir), std::end(rir));
    s_feedback_conv.SetIR(rir_mut);

    // ── miniaudio full-duplex ──
    ma_device_config config = ma_device_config_init(ma_device_type_duplex);
    config.capture.format    = ma_format_f32;
    config.capture.channels  = 1;
    config.playback.format   = ma_format_f32;
    config.playback.channels = 1;
    config.sampleRate        = (ma_uint32)kSampleRate;
    config.dataCallback      = MaCallback;
    config.pUserData         = nullptr;
    config.periodSizeInMilliseconds = 10;

    ma_device device;
    ma_result result = ma_device_init(nullptr, &config, &device);
    if (result == MA_SUCCESS) {
        ma_device_start(&device);
        g_capturing.store(true, std::memory_order_relaxed);
    } else {
        TraceLog(LOG_WARNING, "miniaudio init failed — running in silent mode");
    }

    // ── Knob setup (before loop) ──
    constexpr int kKnobW = 55, kKnobH = 60, kKnobPad = 12;
    int kx_start = (kWindowWidth - (8 * kKnobW + 7 * kKnobPad)) / 2;
    int kx = kx_start, ky = kWindowHeight - 80;

    auto setup_knob = [&](Knob& knob, const char* title,
                           float vmin, float vmax, float vstep, float vdef) {
        knob.set_bound(kx, ky, kKnobW, kKnobH)
            .set_title(title)
            .set_range(vmin, vmax, vstep, vdef)
            .set_value(vdef)
            .set_name_font_size(9)
            .set_number_font_size(9)
            .set_fore_color(Color{200, 200, 210, 255})
            .set_bg_color(Color{30, 30, 35, 255});
        kx += kKnobW + kKnobPad;
    };

    Knob knob_min_freq, knob_ptpr, knob_papr, knob_q_max, knob_gain_min, knob_rel_max, knob_attack, knob_fb_gain;

    kx = kx_start;
    setup_knob(knob_min_freq, "MinFreq", 50, 1000, 10, 200);
    knob_min_freq.value_to_text_function = [](float v) { return TextFormat("%.0f Hz", v); };
    knob_min_freq.on_value_change = [](float v) { s_min_howl_freq.store(v, std::memory_order_relaxed); };

    setup_knob(knob_ptpr, "PTPR", -60, 0, 1, -20);
    knob_ptpr.value_to_text_function = [](float v) { return TextFormat("%.0f dBFS", v); };
    knob_ptpr.on_value_change = [](float v) { s_ptpr_threshold.store(v, std::memory_order_relaxed); };

    setup_knob(knob_papr, "PAPR", 0, 40, 1, 10);
    knob_papr.value_to_text_function = [](float v) { return TextFormat("%.0f dB", v); };
    knob_papr.on_value_change = [](float v) { s_papr_threshold.store(v, std::memory_order_relaxed); };

    setup_knob(knob_q_max, "Q max", 1, 50, 1, 10);
    knob_q_max.value_to_text_function = [](float v) { return TextFormat("%.0f", v); };
    knob_q_max.on_value_change = [](float v) { s_q_max.store(v, std::memory_order_relaxed); };

    setup_knob(knob_gain_min, "Gain min", -80, -20, 5, -60);
    knob_gain_min.value_to_text_function = [](float v) { return TextFormat("%.0f dB", v); };
    knob_gain_min.on_value_change = [](float v) { s_gain_min_db.store(v, std::memory_order_relaxed); };

    setup_knob(knob_rel_max, "Rel max", 100, 20000, 100, 5000);
    knob_rel_max.value_to_text_function = [](float v) { return v >= 1000 ? TextFormat("%.1fs", v/1000) : TextFormat("%.0f ms", v); };
    knob_rel_max.on_value_change = [](float v) { s_rel_max_ms.store(v, std::memory_order_relaxed); };

    setup_knob(knob_attack, "Attack", 0, 200, 5, 10);
    knob_attack.value_to_text_function = [](float v) { return TextFormat("%.0f ms", v); };
    knob_attack.on_value_change = [](float v) { s_attack_ms.store(v, std::memory_order_relaxed); };

    setup_knob(knob_fb_gain, "FB gain", -80, 20, 1, -80);
    knob_fb_gain.value_to_text_function = [](float v) { return TextFormat("%.0f dB", v); };
    knob_fb_gain.on_value_change = [](float v) { s_feedback_gain_db.store(v, std::memory_order_relaxed); };

    // Force knob values to atomics
    s_min_howl_freq.store(knob_min_freq.get_value(), std::memory_order_relaxed);
    s_ptpr_threshold.store(knob_ptpr.get_value(), std::memory_order_relaxed);
    s_papr_threshold.store(knob_papr.get_value(), std::memory_order_relaxed);
    s_q_max.store(knob_q_max.get_value(), std::memory_order_relaxed);
    s_gain_min_db.store(knob_gain_min.get_value(), std::memory_order_relaxed);
    s_rel_max_ms.store(knob_rel_max.get_value(), std::memory_order_relaxed);
    s_attack_ms.store(knob_attack.get_value(), std::memory_order_relaxed);
    s_feedback_gain_db.store(knob_fb_gain.get_value(), std::memory_order_relaxed);

    // ── main loop ──
    while (!WindowShouldClose()) {
        BeginDrawing();
        ClearBackground(Color{10, 10, 12, 255});

        // ── title ──
        DrawText("Auto Notch: Real-Time Howling Suppression", 20, 10, 16, LIGHTGRAY);

        // ── status ──
        {
            int active = g_notch.GetNumActive();
            const char* status = g_capturing.load(std::memory_order_relaxed) ? "● REC" : "■ STOP";
            Color stCol = g_capturing.load(std::memory_order_relaxed) ? GREEN : RED;

            DrawText(TextFormat("Notches: %d / %d", active, AutoNotch::max_notches), 20, 40, 14, WHITE);
            DrawText(status, kWindowWidth - 80, 10, 16, stCol);
        }

        // ── notch frequency display ──
        {
            int active = g_notch.GetNumActive();
            int y_base = 70;
            DrawText("Active Notch Filters:", 20, y_base, 12, GRAY);
            y_base += 20;

            if (active == 0) {
                DrawText("  (none)", 20, y_base, 12, DARKGRAY);
            } else {
                // Horizontal frequency axis
                int ax_x = 40;
                int ax_y = y_base + 30;
                int ax_w = kWindowWidth - 80;

                DrawLine(ax_x, ax_y, ax_x + ax_w, ax_y, Color{60, 60, 70, 255});
                DrawText("20 Hz", ax_x, ax_y + 6, 10, DARKGRAY);
                DrawText("20 kHz", ax_x + ax_w - 30, ax_y + 6, 10, DARKGRAY);

                for (int i = 0; i < active; ++i) {
                    float freq_hz = g_notch.GetNotchFreq(i);
                    float gain_db = g_notch.GetNotchGain(i);

                    // Map frequency to x-axis (log scale)
                    float norm = (std::log10(std::max(freq_hz, 20.0f)) - std::log10(20.0f))
                               / (std::log10(20000.0f) - std::log10(20.0f));
                    int x = ax_x + static_cast<int>(norm * ax_w);
                    int y = ax_y;

                    // Depth indicator: deeper notch = taller line
                    float depth = std::clamp(-gain_db / 60.0f, 0.2f, 1.0f);
                    int bar_h = static_cast<int>(20 + depth * 30);

                    // Color: strong = red, weak = yellow
                    Color c = Color{
                        (unsigned char)(255),
                        (unsigned char)(255 - depth * 200),
                        (unsigned char)(50),
                        255
                    };

                    DrawLine(x, y - bar_h, x, y, c);
                    DrawCircle(x, y - bar_h, 4, c);

                    // Label
                    const char* label = freq_hz >= 1000
                        ? TextFormat("%.1fk", freq_hz / 1000.0f)
                        : TextFormat("%.0f", freq_hz);
                    DrawText(label, x - 16, y - bar_h - 18, 10, c);
                }

            }
        }

        // ── spectrum plots (before / after) ──
        int sp_x = 40;
        int sp_y = 190;
        int sp_w = kWindowWidth - 80;
        int sp_h = 48;
        int sp_gap = 4;
        {
            int _sp_y = sp_y;
            auto draw_spectrum = [&](const char* label, std::function<float(int)> getter, Color col) {
                int cy = _sp_y;
                // Background
                DrawRectangle(sp_x, cy, sp_w, sp_h, Color{15, 15, 18, 255});
                DrawRectangleLines(sp_x, cy, sp_w, sp_h, Color{50, 50, 55, 255});

                // dB reference lines
                for (int db = -80; db <= 0; db += 20) {
                    float t = 1.0f - (db + 80) / 80.0f;
                    int ly = cy + static_cast<int>(t * sp_h);
                    DrawLine(sp_x, ly, sp_x + sp_w, ly, Color{25, 25, 30, 255});
                }

                // Magnitude line
                int prev_x = sp_x, prev_y = cy + sp_h;
                for (int i = 0; i < AutoNotch::GetSlen(); ++i) {
                    float freq_hz = i * kSampleRate / AutoNotch::fft_size;
                    float norm = (std::log10(std::max(freq_hz, 20.0f)) - std::log10(20.0f))
                               / (std::log10(kSampleRate / 2) - std::log10(20.0f));
                    int cx = sp_x + static_cast<int>(norm * sp_w);
                    float db = getter(i);
                    float db_clamped = std::clamp(db, -80.0f, 0.0f);
                    int cy2 = cy + static_cast<int>((1.0f - (db_clamped + 80) / 80.0f) * sp_h);
                    DrawLine(prev_x, prev_y, cx, cy2, col);
                    prev_x = cx;
                    prev_y = cy2;
                }

                DrawText(label, sp_x + 3, cy + 2, 9, col);

                // Notch markers on spectrum
                for (int i = 0; i < g_notch.GetNumActive(); ++i) {
                    float freq_hz = g_notch.GetNotchFreq(i);
                    float norm = (std::log10(std::max(freq_hz, 20.0f)) - std::log10(20.0f))
                               / (std::log10(kSampleRate / 2) - std::log10(20.0f));
                    int mx = sp_x + static_cast<int>(norm * sp_w);
                    DrawLine(mx, cy, mx, cy + sp_h, Color{255, 100, 100, 60});
                }

                _sp_y += sp_h + sp_gap;
            };

            draw_spectrum("Before", [](int i) { return g_notch.GetSpectrumBefore(i); }, Color{80, 180, 255, 200});
            draw_spectrum("After",  [](int i) { return g_notch.GetSpectrumAfter(i); },  Color{80, 255, 120, 200});
        }

        // ── notch details table ──
        {
            int tbl_y = sp_y + 2 * (sp_h + sp_gap) + 8;
            int active = g_notch.GetNumActive();
            if (active > 0) {
                DrawText(" #   Freq(Hz)   Gain(dB)    Q    Release", 20, tbl_y, 9, GRAY);
                tbl_y += 12;
                for (int i = 0; i < active && i < AutoNotch::max_notches; ++i) {
                    float f = g_notch.GetNotchFreq(i);
                    float g = g_notch.GetNotchGain(i);
                    float q = g_notch.GetNotchQ(i);
                    int r = g_notch.GetNotchRelease(i);
                    int tr = g_notch.GetNotchTotalRelease(i);
                    Color rowCol = (i % 2 == 0) ? Color{200, 200, 200, 255} : Color{140, 140, 140, 255};
                    DrawText(TextFormat(" %2d  %7.1f  %7.1f  %5.1f  %3d/%-3d",
                             i + 1, f, g, q, r, tr),
                             20, tbl_y, 9, rowCol);
                    tbl_y += 12;
                }
            }
        }

        // ── knobs row ──
        knob_min_freq.display();
        knob_ptpr.display();
        knob_papr.display();
        knob_q_max.display();
        knob_gain_min.display();
        knob_rel_max.display();
        knob_attack.display();
        knob_fb_gain.display();

        // ── controls help ──
        {
            int y = kWindowHeight - 18;
            DrawText("Controls: [SPACE] Toggle capture  [R] Reset  [ESC] Quit", 20, y, 10, DARKGRAY);
            if (result != MA_SUCCESS) {
                DrawText("WARNING: miniaudio init failed — no audio device", 20, y + 14, 10, RED);
            }
        }

        EndDrawing();

        // ── keyboard ──
        if (IsKeyPressed(KEY_SPACE)) {
            if (g_capturing.load(std::memory_order_relaxed)) {
                if (result == MA_SUCCESS) ma_device_stop(&device);
                g_capturing.store(false, std::memory_order_relaxed);
            } else {
                if (result == MA_SUCCESS) ma_device_start(&device);
                g_capturing.store(true, std::memory_order_relaxed);
            }
        }
        if (IsKeyPressed(KEY_R)) {
            g_notch.Reset();
            s_feedback_conv.Reset();
            s_feedback_buf.clear();
        }
    }

    // ── cleanup ──
    if (result == MA_SUCCESS) {
        ma_device_stop(&device);
        ma_device_uninit(&device);
    }
    CloseWindow();
    return 0;
}
